Процедура Тест() Экспорт 
	
	Для Сч = 0 По 10 Цикл
		
		Попытка
			Док = Документы.ТипВсеСсылки().ПривестиЗначение(); 	
			Ссылка = Док.Ссылка.Ссылка;	
			ДокОбъект = Ссылка.ПолучитьОбъект();
		Исключение
		КонецПопытки;
		
	КонецЦикла;
	
КонецПроцедуры

АдресВнешнейОбработки = ИспользуемоеИмяФайла; // Запомним путь, от куда была запущена обработка.
